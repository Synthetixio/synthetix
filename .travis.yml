sudo: enabled
language: node_js
node_js:
  - 10.16.3
env:
  - COVERAGE=1
  - GAS=1
script:
  - if [ "$COVERAGE" = "1" ]; then npm run coverage --max-old-space-size=4096 && bash <(curl -s https://codecov.io/bash); fi
  - if [ "$GAS" = "1" ]; then npm run test:gas && npx codechecks; fi
