sudo: enabled
language: node_js
node_js:
  - 10.16.3
before_install:
  - npm ci
jobs:
  include:
    - stage: Coverage & Gas Reports
      # run tests with solidity-coverage Upload coverage to codecov.io
      script: npm run coverage && bash <(curl -s https://codecov.io/bash)
       # now test for gas usage and report gas usage back to GitHub
      script: npm test && npx codechecks
    - stage: Afterwards
      script: echo "Why is this even necessary? Can't get two jobs to run in parallel without a second stage it seems"
