name = "chainlink"
version = "0.8-mock"
description = "Deploys a testing aggregator with a given rate dated into the future"

[setting.rate]

[setting.timestamp]
# by default, its dated into the future
defaultValue = "<%= timestamp %>"

[setting.salt]
defaultValue = "default"

[contract.aggregator]
artifact = "MockAggregatorV2V3"
salt = "<%= settings.salt %>"

[invoke.set_answer]
addresses = ["<%= outputs.self.contracts.aggregator.address %>"]
abi = ["<%= outputs.self.contracts.aggregator.abi %>"]

func = "setLatestAnswer"
args = ["<%= settings.rate %>", "<%= settings.timestamp %>"]

step = 1